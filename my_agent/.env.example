# Gemini (either name works; GOOGLE_API_KEY wins if both are set)
GOOGLE_API_KEY=
GEMINI_API_KEY=

# Optional: only if you experiment with LiteLLM/Groq models
GROQ_API_KEY=

# GitHub classic PAT (public repos; improves rate limits)
GITHUB_TOKEN=
